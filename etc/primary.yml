mount-dir: "./var/primary/mount"

data-dir: "./var/primary/data"

exec: "hypercorn games --reload --debug --bind games.local.gd:$PORT --access-logfile - --error-logfile - --log-level DEBUG"

http:
  addr: ":5100"

static:
  primary: true
  hostname: "tuffix-vm"
  advertise-url: "http://localhost:5100"